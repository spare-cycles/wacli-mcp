{
  "extends": "./tsconfig.json",
  "include": ["src/**/*.ts"],
  "//": "Test scaffolding that is not a *.test.ts file gets named here explicitly, because the glob below cannot catch it. Left in, it compiles into dist/ and ships in the image as dead code that nothing imports.",
  "exclude": ["src/**/*.test.ts"]
}
